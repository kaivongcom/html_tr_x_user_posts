# HTML TR for X User Posts
Example `json`, makes `tr` `HTML` nodes (rows) for tweet Posts from x.com account online.

Created in Ruby programming language [branch](https://github.com/kaivongcom/html_tr_x_user_posts).
